import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DesbanearusuariosPage } from './desbanearusuarios.page';

describe('DesbanearusuariosPage', () => {
  let component: DesbanearusuariosPage;
  let fixture: ComponentFixture<DesbanearusuariosPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(DesbanearusuariosPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
