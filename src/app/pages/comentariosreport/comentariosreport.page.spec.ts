import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ComentariosreportPage } from './comentariosreport.page';

describe('ComentariosreportPage', () => {
  let component: ComentariosreportPage;
  let fixture: ComponentFixture<ComentariosreportPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ComentariosreportPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
